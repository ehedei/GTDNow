<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.iespuertodelacruz.dam.gtdnow">

    <application
        android:name=".application.GTDNow"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.DisplayerTaskActivity"></activity>
        <activity android:name=".activity.SelectorProjectActivity" />
        <activity android:name=".activity.DisplayerPlaceActivity" />
        <activity android:name=".activity.DisplayerNoteActivity" />
        <activity android:name=".activity.DisplayerGroupActivity" />
        <activity
            android:name=".activity.EditPlaceActivity"
            android:theme="@style/Theme.MaterialComponents.Light.Dialog" />
        <activity
            android:name=".activity.EditProjectActivity"
            android:theme="@style/Theme.MaterialComponents.Light.Dialog" />
        <activity
            android:name=".activity.EditTaskActivity"
            android:theme="@style/Theme.MaterialComponents.Light.Dialog" />

    </application>

</manifest>